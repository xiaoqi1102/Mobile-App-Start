/**
 * Created by yzsoft on 16/6/28.
 */
const initialState = {
    apiHostUrl: 'localhost:4000'
};

export default (state=initialState,action)=>{
    switch (action.type){
        default:
            return state;
    }
};